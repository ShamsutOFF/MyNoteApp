<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/note_card_view"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/default_padding"
    android:orientation="vertical"
    app:cardBackgroundColor="@color/brown_2"
    app:cardCornerRadius="@dimen/note_card_radius"
    app:cardElevation="10dp"
    app:contentPadding="@dimen/default_padding">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/note_item_title_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/note_item_title_text_size"
            android:textStyle="bold"
            tools:text="Я заголовок" />

        <TextView
            android:id="@+id/note_item_date_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/note_item_date_text_size"
            android:textStyle="italic"
            tools:text="Дата создания" />

    </LinearLayout>

</androidx.cardview.widget.CardView>